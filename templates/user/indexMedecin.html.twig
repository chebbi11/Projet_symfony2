{% extends 'base1.html.twig' %}

{% block sidebar %}
{% endblock %}
{% block body %}
<main id="main" class="main">
<section class="section profile">
    <div class="row">
        <div class="col-xl-8">
        <div class="card">
            <div class="card-body profile-card pt-4 d-flex flex-column align-items-center">
                <h2 class="card-title">Bienvenue docteur</h2>
                <br>
              <img src="{{asset('images/pharmacist.png')}}" alt="Profile" class="rounded-circle">
              <h2>{{ app.user.nomUser }} {{ app.user.prenomUser }}</h2>
              <h3>{{ app.user.roles[0] }}</h3>
              <div class="social-links mt-2">
                <a href="#" class="twitter"><i class="bi bi-twitter"></i></a>
                <a href="#" class="facebook"><i class="bi bi-facebook"></i></a>
                <a href="#" class="instagram"><i class="bi bi-instagram"></i></a>
                <a href="#" class="linkedin"><i class="bi bi-linkedin"></i></a>
              </div>
              <br>
              <br>
                    <center>
                        <button type="button" class="btn btn-success rounded-pill">Fiches Patients</button>
                        
                        <button type="button" class="btn btn-warning rounded-pill">Rendez-vous</button>
                        
                        <button type="button" class="btn btn-info rounded-pill">Événements</button>
                        
                        <button type="button" class="btn btn-danger rounded-pill">Réclamations</button>  
                    </center> 
            </div>
          </div>
        </div>
    </div>
</section>
        
</main>

{% endblock %}